# YiChat Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# ============================================
# FIREBASE CONFIGURATION (Required)
# ============================================
# Get these from Firebase Console > Project Settings > Your apps
# IMPORTANT: Use platform-specific credentials for each platform
# Note: Firebase config is PUBLIC by design (protected by Security Rules)

# Web App Credentials (from Web app in Firebase Console)
FIREBASE_API_KEY_WEB=your-web-api-key-here
FIREBASE_APP_ID_WEB=1:123456789012:web:abcdef1234567890

# Android App Credentials (from Android app in Firebase Console)
FIREBASE_API_KEY_ANDROID=your-android-api-key-here
FIREBASE_APP_ID_ANDROID=1:123456789012:android:abcdef1234567890

# iOS App Credentials (from iOS app in Firebase Console)
FIREBASE_API_KEY_IOS=your-ios-api-key-here
FIREBASE_APP_ID_IOS=1:123456789012:ios:abcdef1234567890

# Shared Firebase Configuration
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012

# ============================================
# AI CONFIGURATION (Cloud Functions Only - DO NOT PUT IN CLIENT!)
# ============================================
# These should ONLY be set in Firebase Cloud Functions config
# NEVER add these to .env.local or commit them!
# Set with: firebase functions:config:set openai.key="your-key"

# OPENAI_API_KEY=sk-...                    # Cloud Functions only!
# PINECONE_API_KEY=...                     # Cloud Functions only!
# PINECONE_ENVIRONMENT=us-east-1-aws       # Cloud Functions only!
# PINECONE_INDEX_NAME=yichat-embeddings    # Cloud Functions only!

# ============================================
# GOOGLE SIGN-IN (Optional - Not in MVP)
# ============================================
# Required only if implementing Google OAuth
# Get from Google Cloud Console > APIs & Services > Credentials

# EXPO_PUBLIC_GOOGLE_CLIENT_ID_IOS=your-ios-client-id.apps.googleusercontent.com
# EXPO_PUBLIC_GOOGLE_CLIENT_ID_ANDROID=your-android-client-id.apps.googleusercontent.com
# EXPO_PUBLIC_GOOGLE_CLIENT_ID_WEB=your-web-client-id.apps.googleusercontent.com

# ============================================
# EXPO CONFIGURATION (Optional)
# ============================================
# These are set automatically by EAS CLI
# Only needed for advanced configuration

# EXPO_PUBLIC_PROJECT_ID=your-expo-project-id
# EAS_PROJECT_ID=your-eas-project-id

# ============================================
# GOOGLE SERVICES (For Firebase Push Notifications)
# ============================================
# Download these files from Firebase Console > Project Settings > Your apps
# - google-services.json (Android) -> Place in project root
# - GoogleService-Info.plist (iOS) -> Place in project root
# Both files are in .gitignore - they contain Firebase config (public but gitignored)
# No environment variables needed - Expo finds them automatically!

# ============================================
# DEVELOPMENT (Optional)
# ============================================
# Uncomment for development-specific settings

# EXPO_PUBLIC_ENV=development
# EXPO_PUBLIC_DEBUG_MODE=true
# EXPO_PUBLIC_USE_FIREBASE_EMULATOR=false

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in Firebase values from Firebase Console > Project Settings > Your apps
#    - Create/select Web app to get web credentials
#    - Create/select Android app to get Android credentials
#    - Create/select iOS app to get iOS credentials
# 3. Run: npm start
# 4. For AI features: Set keys in Firebase Functions config (see CLAUDE.md)
#
# Why Platform-Specific Credentials?
# - Cloud Functions verify which app is calling using the App ID
# - Using wrong credentials (e.g., web on Android) causes "app verification missing" errors
# - The code automatically selects the correct credentials based on Platform.OS
#
# Security Notes:
# - .env.local is in .gitignore (NEVER commit it!)
# - Firebase API keys and App IDs are public (protected by Firebase Security Rules)
# - AI keys (OpenAI, Pinecone) must stay in Cloud Functions (never in client code)
# - google-services.json is in .gitignore (best practice, even though contents are public)
